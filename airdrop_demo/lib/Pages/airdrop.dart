import 'package:flutter/material.dart';

class AirdropPage extends StatelessWidget {
  const AirdropPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Airdrop');
  }
}